machine:
  timezone:
    America/New_York

  java:
    version: oraclejdk8

general:
   artifacts:
     - $CIRCLE_TEST_REPORTS
     

test:
  
  pre:
    - mkdir -p $CIRCLE_TEST_REPORTS/reports
    - eslint ./src/ --format junit --output-file $CIRCLE_TEST_REPORTS/test/test-results.xml
